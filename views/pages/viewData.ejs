<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>View Data</title>
</head>
<body>
    <h1>View Data</h1>
    <ul>
        <% data.forEach(item => { %>
            <li><%= item.name %> - <%= item.email %></li>
        <% }) %>
    </ul>
    <a href="/">Back to Home</a>
</body>
</html>
